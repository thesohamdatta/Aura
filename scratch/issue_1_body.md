## Parent

#9

## What to build

Build the full-bleed `#app-demo-section` container on `index.html` with a `#f5f5f7` background. Add the centered headline text block and insert the mockup image asset `assets/png/mobile and device.png`. Include mobile styles to stack components and adjust margins.

## Acceptance criteria

- [ ] HTML container is present on `index.html` after the highlights grid.
- [ ] Section container has a background color of `#f5f5f7` and centered layout.
- [ ] Image element `#demoPhoneImage` is configured with `assets/png/mobile and device.png` as src.
- [ ] Layout behaves responsively on mobile viewports (< 768px).

## Blocked by

None - can start immediately
