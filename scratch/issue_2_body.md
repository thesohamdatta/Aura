## Parent

#9

## What to build

Apply the dual `filter: drop-shadow` floating shadow properties to the mockup image. Add an `IntersectionObserver` script to trigger the fade-up slide entrance animations on viewport enter (image → caption label → body copy).

## Acceptance criteria

- [ ] Mockup image has floating drop-shadow effect using CSS filters.
- [ ] IntersectionObserver is initialized for `#app-demo-section`.
- [ ] Animates phone mockup, caption label, and body copy sequentially when scrolled into view.

## Blocked by

- #10
